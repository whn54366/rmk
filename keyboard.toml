# RMK keyboard.toml configuration file
[keyboard]
name = "whn_ff Keyboard"
product_name = "whn_ff Keyboard"
vendor_id = 0x4c4b
product_id = 0x4643
manufacturer = "whn54366"
serial_number = "vial:f64c2b3c:000001"
chip = "nrf52840"
usb_enable = true

[matrix]
row_pins = ["P1_07", "P1_02", "P1_01"]
col_pins = ["P0_09", "P0_10", "P1_11", "P1_06", "P0_11", "P1_04", "P1_00"]

[layout]
rows = 3
cols = 7
layers = 3  # 支持 MO(1)/MO(2)
# matrix_map 单行格式，RMK 编译要求
matrix_map = "(0,0,R) (0,1,R) (0,2,R) (0,3,R) (0,4,R) (0,5,R) (0,6,R) (1,0,R) (1,1,R) (1,2,R) (1,3,R) (1,4,R) (1,5,R) (1,6,R) (2,0,R) (2,1,R) (2,2,R) (2,3,R) (2,4,R) (2,5,R) (2,6,R)"

# 基础层（层0）：所有按键名去掉 KC_ 前缀，空键用 No
[[layer]]
name = "base_layer"
keys = "F1 F3 F7 No No No MO(1) No W No I O P MO(2) A S D H J K L"

# 层1（MO(1) 对应的层，空层用 No 填充）
[[layer]]
name = "layer1"
keys = "No No No No No No No No No No No No No No No No No No No No No"

# 层2（MO(2) 对应的层，空层用 No 填充）
[[layer]]
name = "layer2"
keys = "No No No No No No No No No No No No No No No No No No No No No"

[ble]
enabled = true
battery_adc_pin = "vddh"
adc_divider_measured = 2000
adc_divider_total = 2806
default_tx_power = 0
ble_use_2m_phy = true

[host]
vial_enabled = true
unlock_keys = [[0, 0], [0, 1]]

[rmk]
mouse_key_interval = 20
mouse_wheel_interval = 80
combo_max_num = 8
combo_max_length = 4
fork_max_num = 8
morse_max_num = 8
max_patterns_per_key = 36
macro_space_size = 256
debounce_time = 20
event_channel_size = 16
controller_channel_size = 16
controller_channel_pubs = 12
controller_channel_subs = 8
report_channel_size = 16
vial_channel_size = 4
flash_channel_size = 4
split_peripherals_num = 1
ble_profiles_num = 3
split_central_sleep_timeout_seconds = 0

[chip.nrf52840]
dcdc_reg0 = true
dcdc_reg1 = true
dcdc_reg0_voltage = "3V3"
